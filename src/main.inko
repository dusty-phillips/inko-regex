import regex::parser::(Parser)
import std::stdio::(STDOUT)

class async Main {
  fn pub async main() {
    let parser = Parser.new("ab")
    let tree = parser.parse()
    STDOUT.new.print(tree.debug(""))
    STDOUT.new.print("------------")
  }
}
